node_modules
dist
.next
coverage
pnpm-lock.yaml
*.lock

# Claude Code & agent skill files — not project source
.claude
.agents
tasks
spec-driven

# Project documentation — formatted manually
CLAUDE.md
README.md
Escopo_*.md

# Generated by `pnpm --filter api openapi:dump` — must be byte-identical to
# the script's JSON.stringify output so the CI drift gate sees no diff.
openapi/baseline.json
